{{- $venue := . -}}
{{- range hugo.Sites -}}
    {{- range where (where .Pages ".Section" "venues") ".Params.id" $venue -}}
        {{- with .Params.coordinates -}}
            GEO:{{ .latitude }};{{ .longitude -}}
        {{- end -}}
    {{- end -}}
{{- end -}}
