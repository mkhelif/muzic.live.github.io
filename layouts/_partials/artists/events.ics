{{/* --- Events ---  */}}
{{- $events := where .Site.RegularPages "Section" "events" -}}
{{- $events = where $events ".Params.artists" "intersect" (slice .Params.id) -}}
{{- $events = $events.ByDate -}}
{{- partial "calendar/events.ics" (dict "page" . "events" $events) -}}
